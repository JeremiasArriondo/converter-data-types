---
import Layout from "../layouts/Layout.astro";
import Converter from "../components/Converter";
---

<Layout title="Convertidor JSON a Data-Type">
  <main>
    <h1 class="text-4xl font-bold">
      Convertidor de JSON a <span class="text-gradient"> Data-Types</span>
    </h1>
    <div class="my-8 text-base-light">
      <h2 class="text-xl">
        Los <strong>Data Types</strong> son una forma concisa y versátil de describir
        y validar datos dentro de su definición de API.
      </h2>
      <p class="text-md">
        Para comenzar ingresa tu estructura json en el siguiente contenedor<br
        />
      </p>
    </div>
    <div class="instructions">
      <Converter client:load />
    </div>
  </main>
</Layout>

<style>
  main {
    margin: auto;
    padding: 1rem;
    width: 800px;
    max-width: calc(100% - 2rem);
    color: white;
    font-size: 20px;
    line-height: 1.6;
    min-height: 100vh;
    min-height: 100dvh;
  }

  .text-gradient {
    background-image: var(--accent-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-size: 400%;
    background-position: 0%;
  }
  .instructions {
    margin-bottom: 2rem;
    border: 1px solid #93c5fd;
    background: linear-gradient(
      rgba(0, 159, 224, 66%),
      rgba(0, 159, 224, 22%)
    );
    padding: 1.5rem;
    border-radius: 8px;
  }
</style>